Files should be placed in a folder named /mmmr

mmmrLib.php is a PHP library that will calculate the mean, median, mode, and range of a set of numbers.

index.php is an API that will accept a JSON post containing an array of numbers. It will return a json object with the mean, median, mode and range of the numbers.